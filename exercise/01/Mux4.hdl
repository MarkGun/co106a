CHIP Mux4 {
    IN a[4], b[4], sel[2];
    OUT out;

    PARTS:
    // Put your code here:
    Mux (a=a[0],b=b[0],sel=sel,out=c0);
    Mux (a=a[1],b=b[1],sel=sel,out=c1);
    Mux (a=a[2],b=b[2],sel=sel,out=c2);
    Mux (a=a[3],b=b[3],sel=sel,out=c3);
    Mux (a=c0,b=c1,sel=sel,out=c4);
    Mux (a=c2,b=c3,sel=sel,out=c5);
    Mux (a=c4,b=c5,sel=sel,out=out);

}